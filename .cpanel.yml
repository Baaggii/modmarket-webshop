# .cpanel.yml  – minimal, “fire-and-forget”
---
deployment:
  tasks:
    - /bin/bash -lc 'npm ci --omit=dev --no-progress'
    - /bin/bash -lc 'mkdir -p tmp && touch tmp/restart.txt >/dev/null 2>&1 &'
